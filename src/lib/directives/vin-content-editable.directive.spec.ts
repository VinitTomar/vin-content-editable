import { VinContentEditableDirective } from './vin-content-editable.directive';

describe('VinContentEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new VinContentEditableDirective();
    expect(directive).toBeTruthy();
  });
});
