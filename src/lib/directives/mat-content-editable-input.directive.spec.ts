import { MatContentEditableInput } from './mat-content-editable-input.directive';

describe('MatContentEditableInput', () => {
  it('should create an instance', () => {
    const directive = new MatContentEditableInput(null, null, null);
    expect(directive).toBeTruthy();
  });
});
